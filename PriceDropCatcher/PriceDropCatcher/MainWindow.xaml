<Window x:Class="PriceDropCatcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceDropCatcher"
        mc:Ignorable="d"
        Title="PriceDropCatcher" Height="520" Width="920">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#111827" Padding="14">
            <DockPanel LastChildFill="True">
                <TextBlock Foreground="White" FontSize="18" FontWeight="SemiBold" Text="PriceDropCatcher" />
                <TextBlock DockPanel.Dock="Right" Foreground="#D1D5DB" FontSize="12" VerticalAlignment="Center"
                           Text="{Binding StatusText}" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Tracked products" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListView Grid.Row="0" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Added" Width="140" DisplayMemberBinding="{Binding AddedAt}" />
                                <GridViewColumn Header="Title" Width="340" DisplayMemberBinding="{Binding Title}" />
                                <GridViewColumn Header="Host" Width="180" DisplayMemberBinding="{Binding Host}" />
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Width="110" Margin="0,0,8,0" Content="Copy URL" Command="{Binding CopyUrlCommand}" />
                        <Button Width="110" Content="Remove" Command="{Binding RemoveSelectedCommand}" />
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Details">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Title" Foreground="#6B7280"/>
                    <TextBlock Grid.Row="1" Text="{Binding SelectedProduct.Title}" FontSize="14" TextWrapping="Wrap" Margin="0,2,0,10"/>

                    <TextBlock Grid.Row="2" Text="Price" Foreground="#6B7280"/>
                    <TextBlock Grid.Row="3" Text="{Binding SelectedPriceText}" FontSize="14" Margin="0,2,0,10"/>

                    <TextBlock Grid.Row="4" Text="URL" Foreground="#6B7280"/>
                    <TextBox Grid.Row="5" Text="{Binding SelectedProduct.Url}" IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" Background="#F9FAFB" BorderBrush="#E5E7EB"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
